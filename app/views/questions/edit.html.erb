<div class="title-section">
  <div class="breadcrumb">
    <%= link_to(@question_set.name, question_set_path(@question_set.id)) %> / <%= t("questions.title") %>
  </div>
  <br>
  <div class="lead text-center"><%= %(#{@question_set.name} - #{t("question")} #{@question_set.questions.length + 2}) %> </div>
</div>

<div class="content-section">
  <%= render :partial => "edit_form" %>
</div>
