<div class="title-section">
  <div class="breadcrumb">
    <%= link_to(t("question_sets.index.title"), question_sets_path) %> / <%= link_to(@question_set.name, question_set_path(@question_set.id)) %> / <%= t("questions.title") %>
  </div>
  <div class="lead text-center"><%= %(#{@question_set.name} - #{t("question")}  #{@question_set.questions.length}) %> </div>
</div>

<div class="content-section">
  <%= render :partial => "form" %>
</div>
